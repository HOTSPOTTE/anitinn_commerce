# Database (PostgreSQL is the default; set DB_ENGINE=sqlite to use db.sqlite3)
POSTGRES_DB=anitinn
POSTGRES_USER=anitinn
POSTGRES_PASSWORD=anitinn
POSTGRES_HOST=localhost
POSTGRES_PORT=5432   # docker compose; use 5434 if running a local .pgdata instance
